apiVersion: apps/v1
kind: Deployment
metadata:
  name: driver-mobile-app
  namespace: fleet-ai
spec:
  replicas: 2
  selector:
    matchLabels: { app: driver-mobile-app }
  template:
    metadata:
      labels: { app: driver-mobile-app }
    spec:
      containers:
        - name: driver-mobile-app
          image: ghcr.io/your-org/fleet-ai/driver-mobile-app:latest
          ports: [{ containerPort: 80 }]
          readinessProbe:
            httpGet: { path: /, port: 80 }
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
