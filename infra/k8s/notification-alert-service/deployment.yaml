apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-alert-service
  namespace: fleet-ai
spec:
  replicas: 2
  selector:
    matchLabels: { app: notification-alert-service }
  template:
    metadata:
      labels: { app: notification-alert-service }
    spec:
      containers:
        - name: notification-alert-service
          image: ghcr.io/your-org/fleet-ai/notification-alert-service:latest
          ports: [{ containerPort: 8084 }]
          envFrom:
            - configMapRef: { name: fleetai-common-config }
          env:
            - { name: SERVER_PORT, value: "8084" }
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom: { secretKeyRef: { name: fleetai-secrets, key: POSTGRES_PASSWORD } }
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits: { cpu: "1", memory: "1Gi" }
